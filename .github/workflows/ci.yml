# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json

# The checks come from package.json's `check` script — which here ends in
# `pnpm validate`, a repo-specific step the body picks up without being told.
name: CI

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened, ready_for_review]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - LICENSE

permissions:
  contents: read

jobs:
  check:
    name: CI
    uses: kirchDev/workflows/.github/workflows/_ci-check.yml@f47f598f549a3c97924fdfd00f15371ea557e1aa # v0.1.1
